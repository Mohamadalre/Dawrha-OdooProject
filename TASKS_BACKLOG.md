# TASKS_BACKLOG — المهام المتبقية

> **اقرأ هذا الملف في بداية أي جلسة** لمعرفة ما لم يُنفَّذ بعد.
> كل مهمة مكتوبة بحيث تُنفَّذ بلا الرجوع للمحادثة: الملف، السطر، والسلوك المطلوب.
> آخر تحديث: 2026-08-03

## الحالة

| # | المهمة | الحالة |
|---|---|---|
| 1 | المزامنة الآلية بين النظامين | ✅ **منجَز ومُتحقَّق** |
| 2 | موظف الاستقبال — رفع صورة QR للشحنة | ⬜ لم يبدأ |
| 3 | التوكن والصلاحيات حسب حالة الحساب + أشكال الاستجابة | ⬜ لم يبدأ |
| 4 | المفضلة — الأدوار المسموح لها | ⬜ لم يبدأ |
| 5 | أسعار الزائر حسب التطبيق | ⬜ لم يبدأ |
| 6 | ثغرات أمنية متبقية | ⬜ لم يبدأ |
| 7 | لوحة قاعدة البيانات — ما تبقّى | ⬜ لم يبدأ |

---

## ١ — المزامنة ✅ منجَز (جرد كامل، لا عيّنة)

انظر `PROJECT_LOG.md` (جلسات 2026-08-03 أ/ب/ج) — وفيها **المصفوفة الكاملة**:
كل قناة تبادل في الاتجاهين وغطاؤها. سبعة مسارات مصالحة.

`npx ts-node -r tsconfig-paths/register reconcile-check.ts` يشغّلها كلها يدوياً.

**عمل يدوي متبقٍّ عليك:** ثلاثة يتامى في أودو بلا مقابل بالباك — احذفهم من أودو:
`recycle.product.category id=2 (Batteries)` · `recycle.product id=1 (زجاجات PET7)`
· `recycle.product id=2 (AA Batteries)`. (المُصالِح يبلّغ ولا يحذف عمداً — الحذف
من مقارنة مجموعات لا رجعة فيه ونطاقه الكتالوج كله.)

**القاعدة الذهبية:** أي قناة تبادل جديدة تحتاج مُصالِحاً، وإلا فهي الفجوة القادمة.

### بند صغير متبقٍّ في المزامنة
`CANCEL_SHIFT_CHANGE` يحذف الطلب من **الطرفين**، فلا يبقى صفٌّ محلي يدلّ على
فشل الحذف. يحتاج مقارنة مجموعات كاملة مثل اليتامى: اقرأ طلبات تغيير الوردية
`pending` من أودو وقابلها بـ`shift_change_requests` — ما لا مقابل له = حذف ضاع.
أضِفه إلى `OrphanReconcileService` (النمط جاهز). أثره محدود: طلب معلّق يراه
المدير وقد سحبه السائق.

### ملاحظة جانبية للفحص
هجرتان بنفس الطابع الزمني `1786700000000` (`favouritesMigration` و
`warehouseShipmentCountMigration`) — ترتيب تنفيذهما غير محدَّد. تحقّق.

---

## ٢ — موظف الاستقبال: رفع صورة QR لا يعمل

**المطلوب:** إصلاح رفع صورة الـQR code الخاصة بالشحنة عند موظف الاستقبال،
ثم **فحص كامل مسار موظف الاستقبال** بحثاً عن أي شاشة/زر لا يعمل.

**من أين تبدأ:**
- `addons/recycle_warehouse/models/shipment.py`
- `addons/recycle_warehouse/controllers/dashboard_api.py` (راوتات الاستقبال)
- `addons/recycle_warehouse/static/src/js/` — ابحث عن لوحة الاستقبال
  (لا يوجد `dashboard_receiving.xml` صراحةً؛ تحقّق أي ملف يخدم هذا الدور)
- `addons/recycle_warehouse/views/shipment_views.xml`

**لم يُشخَّص بعد** — لم أفتح المسار في هذه الجلسة، فلا تفترض سبباً.

**التحقق المطلوب:** اختبار يرفع صورة فعلاً ويؤكّد حفظها وقراءتها، لا فحص بصري فقط.

---

## ٣ — التوكن والصلاحيات حسب حالة الحساب

### ⚠️ الترتيب إلزامي — لا تُصدر التوكن قبل إتمام الحارس

**قياس فعلي:** 240 راوتاً، **٣٠ فقط** تحمل `@AccountsStatus`. والحارس كان يفشل
مفتوحاً (`if (!requiredAccountStatus) return true`) — أي أن **٢١٠ راوتاً** تقبل
أي حالة. هذا آمن اليوم **بالصدفة** فقط: لأن `PENDING_APPROVAL`/`NEED_CHANGES`/
`REJECTED` لا يحصلون على توكن. **فإصدار التوكن وحده يفتح لهم ٢١٠ راوتاً فوراً**
(سلة، طلبيات، كتالوج، أسعار).

**✅ أُنجز 2026-08-03:** عُكس الحارس ليفشل **مغلقاً** — راوت بلا ديكوريتر يتطلب
`ACTIVE`، و`BLOCKED` مرفوض قبل استشارة أي قائمة. 7 اختبارات
(`account-status.guard.spec.ts`) + 510 اختباراً كلها خضراء + `BOOT OK`.

**⬜ متبقٍّ قبل إصدار التوكن — بهذا الترتيب:**

1. **التغطية**: `JwtAuthGuard` مطبَّق على مستوى الكنترولر في **39** موضعاً، لا
   عالمياً. وحُرّاس Nest العالمية تعمل **قبل** حُرّاس الكنترولر — فتسجيل
   `AccountStatusGuard` عالمياً سيجعله يقرأ `request.user` قبل ملئه فلا يحمي
   شيئاً. الحل: اجعله يتحقّق من الـJWT بنفسه (بالتوقيع، لا بفكّ الترميز فقط —
   وإلا زوّر أحدهم `accountStatus: ACTIVE`) ثم سجّله عالمياً؛ أو أضِفه يدوياً
   إلى الـ39 `@UseGuards`. **الأول أفضل**: مكان واحد، ويغطي أي راوت جديد.
2. **افتح الراوتات القليلة** التي تحتاجها الحالات غير الفعّالة:
   `PENDING_APPROVAL` → عرض الطلب + خطوات الـonboarding + البروفايل ·
   `NEED_CHANGES` → عرض الطلب + `PATCH /media/:id/reupload` + البروفايل ·
   `REJECTED` → عرض الطلب + البروفايل فقط.
3. **ثم** أصدِر التوكن في `pending-approval.handler.ts` و`needChange.handler.ts`
   و`rejected.handler.ts` (انسخ نمط `active.handler.ts`؛ `generateTokens` يضع
   `accountStatus` في الـJWT أصلاً). `blocked.handler.ts` يبقى بلا توكن.
4. **اختبار e2e** يُثبت أن توكن `PENDING_APPROVAL` يُرفض على السلة والطلبيات
   ويُقبل على عرض الطلب — قبل إعلان الإنجاز.

### أ. إصدار التوكن

كل الأدوار (مؤسسات، معامل، جهات حرة، سائق) تحصل على **access + refresh token**
في هذه الحالات — لا رفض دخول:

| الحالة | ماذا يُسمح له |
|---|---|
| `PENDING_APPROVAL` | عرض طلب تسجيله · تعديل خطوات الـonboarding · عرض بروفايله |
| `NEED_CHANGES` | عرض طلب تسجيله · **إعادة رفع الوثيقة المرفوضة** · عرض بروفايله |
| `REJECTED` | عرض طلبه + بروفايله **فقط** — لا شيء غير ذلك |
| `BLOCKED` | **لا شيء إطلاقاً** (لا توكن، أو توكن مرفوض في كل مكان) |
| `ACTIVE` | كل شيء |

**ممنوع في كل الحالات عدا `ACTIVE`:** تصفّح المواد والتصنيفات، البحث،
الإضافة للسلة، إنشاء طلبية، المفضلة.

**نقطة تحتاج قراراً — رأيي كمنفّذ:** الأنسب حارس (`Guard`) مركزي يقرأ حالة
الحساب من التوكن ويقابلها بقائمة بيضاء من المسارات لكل حالة، بدل توزيع الشرط
على كل كنترولر. القائمة البيضاء تُكتب في مكان واحد (مثل `permissions.ts`)
فتصبح مراجعتها ممكنة، ولا يمكن لمسار جديد أن «ينسى» الحماية.

**من أين تبدأ:** `src/auth/` (الـStrategy Pattern لحالات الحساب:
`handlers[AccountStatus]`) · `src/user/enums/account-status.enum.ts` ·
الحارس الحالي `PermissionsGuard`.

### ب. أشكال الاستجابة (data داخل data)

المطلوب تسمية الكائن الداخلي بدل تعشيش `data`:

| اليندبوينت | الاسم المطلوب |
|---|---|
| رفع الوثائق | `mediaDetails` (فيه الصورة والـid) |
| Add Information | `accountDetails` |
| إضافة الموقع | `locationDetails` |
| إضافة المواد | (نفس النمط — `materialDetails`) |

**وأيضاً:** ضبط الرسائل المرجَعة لتكون واضحة ومفهومة (عربي/إنجليزي عبر i18n).

**انتبه:** العقد الموحّد للاستجابة مثبَّت بـ
`test/response-envelope.e2e-spec.ts` — أي تغيير يجب أن يبقيه أخضر.
**ممنوع `''`** في القيم (استخدم `null`)، وممنوع تعشيش `data` داخل `data`.

**من أين تبدأ:** `src/onboarding/services/onboarding-submission.service.ts` ·
`src/media/media.service.ts` · `src/i18n/{ar,en}/translation.json`.

---

## ٤ — المفضلة

- مسموحة للأدوار: `CITIZEN` · `INSTITUTIONS` · `FACTORY` · `EXTERNAL_PARTNER`.
- **الأدمن**: يستطيع **عرض** مفضلة حساب مُعيَّن فقط — لا إضافة ولا حذف.

**من أين تبدأ:** ابحث عن موديول المفضلة في `src/` (لم أتحقّق من وجوده بعد —
إن لم يكن موجوداً فالمهمة إنشاؤه لا تعديله).

---

## ٥ — أسعار الزائر حسب التطبيق

عند راوتات الزائر (`public`/`guest`) للمواد والعروض:

- **`user-app`** → أسعار دور **المستخدم/المواطن** فقط (`INDIVIDUAL`).
- **`factory-app`** → أسعار دور **المعمل** فقط (`FACTORY`) — **وليس** الجهات الحرة.

**من أين تبدأ:** `src/waste-management/catalog/` (المسارات العامة) ·
خريطة الأدوار→الشرائح في `PROJECT_CONTEXT.md §5`.

**نقطة تحتاج انتباهاً:** يجب أن يصل التطبيق بشيء يميّزه (هيدر أو مسار منفصل).
تحقّق كيف يميّز النظام حالياً بين التطبيقين قبل أن تختار الآلية.

---

## ٦ — ثغرات أمنية متبقية

مرتّبة بالخطورة:

0. ✅ **أُصلح 2026-08-03 — `recycle.api_key` كان المفتاح الافتراضي المنشور.**
   أربع راوتات في `api.py` (منها **إنشاء الطلبيات**) كانت محروسة بكلمة سرّ في
   المستودع. البذرة صارت فارغة (فشل مغلق) ومفتاح 64 محرفاً ضُبط حيّاً.
   **تنبيه للمستقبل:** تغيير `ir.config_parameter` لا يسري على العامل الحيّ
   إلا بإعادة تشغيل حاوية أودو — الكتابة بـSQL و`odoo shell` كلاهما لا يكفي.

1. **توكنات الحضور في `ir.config_parameter`** — نص صريح، بلا انتهاء ولا إبطال.
   `addons/recycle_warehouse/controllers/attendance_api.py::_store_token`.
   المطلوب: تخزين hash + صلاحية زمنية + إبطال عند تسجيل الخروج.
2. **`Access-Control-Allow-Origin: *`** على كل ردود `attendance_api.py`.
3. **لوحة قاعدة البيانات — إضافة اتصال لأي host/port**: تمكّن من مسح الشبكة
   الداخلية من السيرفر. `db-dashboard/backend/src/connections/connections.service.ts`.
4. **كلمات مرور القواعد نصاً صريحاً** في `db-dashboard/backend/connections.json`.
5. **`origin: true`** في CORS + **لا حدّ لمحاولات الدخول** + مقارنة كلمة المرور
   ليست constant-time. `db-dashboard/backend/src/main.ts` و`auth.controller.ts`.

**أُصلح سابقاً** (لا تُعِده): IDOR الحضور · `readonly=False` للراوتات الكاتبة.

---

## ٧ — لوحة قاعدة البيانات: ما تبقّى

**أُصلح:** التعديل يرسل ما تغيّر فقط · تحويل الأنواع (مصفوفات/تواريخ/JSON) ·
التحقق بعد الكتابة · الحذف مع العلاقات في معاملة واحدة.

**لم يُتحقَّق منه بصرياً:** التحقق التفاعلي من الأزرار في المتصفح — يحتاج
تسجيل دخولك أنت (لن أُدخل كلمة مرور الأدمن).

**متبقٍّ للفحص:** `COUNT(*)` على كل صفحة يبطئ الجداول الضخمة · لا حماية من
حذف تعاقبي واسع عدا حدّ 5000 صف.

---

## قرار مغلق: معرّفات أودو تبقى integer — لا تُحوَّل إلى UUID

**السؤال:** هل نجعل `id` في أودو UUID مثل الباك ايند لحماية أفضل؟
**الجواب: لا — غير ممكن عملياً، ولا يشتري أماناً.** لا يُعاد فتحه.

**غير ممكن:** معرّف أودو ليس خيار تنسيق بل بنية الـORM. الفحص على قاعدتك:
كل `id` — في نماذجنا `recycle_*` وفي نواة أودو `res_users`/`ir_model_data` —
`integer` مع `nextval(...)`، و**١٢٨٥ قيد مفتاح أجنبي** كلها على أعداد.
و`ir_model_data` (الذي يربط كل XML id بسجلّه) يخزّن `res_id` عدداً — أي أن
الـviews والـmenus والصلاحيات والتقارير كلها مبنية عليه. تغييره = تفريع نواة
أودو، وكل ترقية رسمية بعدها تصطدم بك.

**ولا يشتري أماناً:** الـUUID يمنع **التخمين المتسلسل**، وهو خطر حين يكون
المعرّف في رابط يراه المستخدم النهائي. معرّفات الباك ايند كذلك فعلاً (تطبيقات
الجوال تراها) — ولهذا هي UUID بحق. أما معرّفات أودو **فلا يراها مستخدم نهائي
إطلاقاً**: المتحدّث الوحيد مع أودو هو الباك ايند عبر JSON-RPC بجلسة خدمة، أو
راوتات `api.py` المحميّة بـ`X-API-KEY` + `compare_digest` + rate limit.
وحدّ الأمان في أودو هو `ir.model.access` + record rules + `has_group` — وهي
مطبّقة (تحقّقتُ من `dashboard_api.py`). معرّف متسلسل خلف تحقّق صلاحيات سليم
ليس ثغرة.

**إن كان القلق الحقيقي هو التعداد**، فمحلّه الباك ايند لا أودو — وهو محلول
هناك بالـUUID أصلاً.

## قواعد ثابتة لا تُخالَف

- **لا بيانات ثابتة (hardcoded)** — كل شيء من الكنترولر/الموديل.
- **كل نص مترجم** عربي/إنجليزي.
- **Odoo سيد الأسطول والمستودعات**؛ الباك ايند مرآة + بوابة تطبيق السائق.
- **الكتابة إلى Odoo عبر الطابور حصراً** + تعويض.
- **أي قناة تبادل جديدة تحتاج مُصالِحاً** — وإلا فهي الفجوة القادمة.
- **التحقق المعتمد:** `npx tsc --noEmit` + `npx jest` +
  `npx jest --config test/jest-e2e.json response-envelope` + `boot-check.ts`.
  وفي أودو: اختبارات على **نسخة** من القاعدة لا على الحيّة.

---

## سيناريو العروض — الحالة بعد جلسة 2026-08-04

### ✅ منجَز ومُختبَر (13 اختباراً في `offer-rules.spec.ts`)

**قواعد الحالة والجمهور** — القاعدة الأساسية في السيناريو:
- معمل/جهة حرة + مادة مصنَّفة بحالات → **يجب** تحديد الحالة، وإلا يُرفض
- مؤسسات/مستخدم → **يُمنع** تحديد حالة (سعر واحد مسطّح)
- عرض بلا جمهور محدَّد → يُمنع تحديد حالة (يصل لمن لا حالات لهم)
- مادة بلا حالات أصلاً → معمل يضع سعراً مسطّحاً (لا شيء ليُسمّى)
- **معمل يملك عروضاً على حالات مختلفة لنفس المادة** ✓
- عرضان حيّان على **نفس** الحالة والجمهور → `409` (لا فائز محدَّد)
- والقواعد تُعاد عند **التعديل** لا الإنشاء فقط (وإلا التُفّ عليها بإعادة توجيه عرض قائم)

### ✅ موجود أصلاً وتحقّقتُ منه بقراءة الكود
- **الانتهاء التلقائي**: كل الاستعلامات تُرشّح `validUntil > NOW()` — فالسعر يعود للطبيعي وحده
- **السلة تستعمل سعر العرض** وتُثبّته لقطةً في `unitPrice` — فالطلبات المنفَّذة تبقى على سعرها القديم
- **تصنيف يعرض كل المواد** — العرض إثراء لا مُرشِّح

### ❌ لم يُنفَّذ بعد

1. **عدة عروض لنفس المادة في الردّ**: `activeOffersForProducts` تُعيد **عرضاً واحداً**
   لكل مادة (`if (!map.has(productId))`). فمادة مصنَّفة عليها ٣ عروض بحالات مختلفة
   تظهر بعرض واحد فقط. يلزم تجميعها كمصفوفة لكل حالة.

2. **`offer_valid_until` غائب عن ردّ المادة**: `mapProduct` يُعيد `has_offer`
   و`offer_price` و`discount_percentage` فقط — لا موعد الانتهاء.

3. **ترتيب العروض بالخصم الحقيقي**: `orderBy('o.discountPercentage','DESC')` يستعمل
   الحقل **المُدخَل يدوياً** من الأدمن، لا الخصم المحسوب مقابل السعر القديم لنفس
   المادة. المطلوب: `(سعر_المادة − سعر_العرض) / سعر_المادة` لكل مادة على حدة،
   بحيث 100→50 تسبق 200→190.

4. **راوتان مخصّصان**: تعديل وقت الانتهاء، وتعديل السعر. (`PUT offers/:id` يغطّيهما
   وظيفياً لكن المطلوب راوتان منفصلان.)

5. **عرض السعر القديم والجديد في أودو**: لم أفحص مسار المزامنة بعد.

---
## واجهة أودو — جلسة 2026-08-06

### ✅ منجَز ومؤكَّد بصرياً/وظيفياً
- **تعديل المستودع: قفل المحافظة** — `province_id` صار `readonly="id"` في
  `warehouse_views.xml`. مؤكَّد: نموذج West Hub33 يعرض المحافظة نصاً للقراءة،
  والإحداثيات قابلة للتعديل. (العنوان `address` غير معروض في النموذج أصلاً.)
- **فلتر المخزون قائمة منسدلة** — حوّلت أزرار الحالة (`o_ra_filter_pill`) إلى
  `<select>` في `dashboard_admin.xml`. مؤكَّد: قائمتان "All Warehouses" +
  "All Conditions"، والفلترة تعمل (11→1 صف عند Excellent).
- **ترجمة خطأ المحافظة والعنوان** — أُضيفت لـ `ar.po`. مؤكَّد عبر shell: الرسالة
  تظهر بالعربية. + "All Conditions" في `recycle_i18n_shared.js`.

### ⚠️ يحتاج توضيحاً من المستخدم (رأيتُ الشاشة، لم أطابق الشكوى)
- **الغيمة فوق النافبار** — على لوحة الأدمن، الشات بوت FAB (`dw-cb-fab`)
  والفقاعات (`o-mail-ChatHub-bubbles`) كلاهما **أسفل** يمين (`bottom:18px`)،
  لا أعلى. أيقونة `fa-comments` في النافبار العلوي لكنها زر لا "غيمة عائمة".
  → على أي واجهة بالضبط تظهر الغيمة في الأعلى؟ (موظف؟ مدير؟)
- **المساحة البيضاء فوق النافبار** — النافبار `o_ra_unified_nav` خلفيته
  `rgba(255,255,255,0.88)` + `o_ra_main` بـ `padding-top:32px`. الفراغ تباعد
  طبيعي. → هل المطلوب إلغاء التباعد أم تغيير لون النافبار؟
- **فلتر اليوم/الشهر** — البيانات موزّعة على أيام (شحنات: اليوم=0 شهر=3 كل=42).
  المنطق سليم؛ الشحنات/الطلبات تُفلتر. بطاقات مستودعات/موظفين/وظائف تعرض
  الإجمالي عمداً (تعليق سطر 560). → أي بطاقة تحديداً "تُظهر نفس القيم"؟

### ⬜ لم أصل إليها (واجهات أخرى تحتاج تنقّلاً)
- تناسق عرض الأسعار تحت العرض + حقل خارج الكرت (واجهة الأسعار)
- زر الرجوع يغطّي الخيارات في البروفايل + شكله responsive
