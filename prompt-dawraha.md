# Prompt: تطوير واجهات موقع "دورها" (منصة إعادة تدوير)

## الدور المطلوب منك (Persona)
تعامل مع هذه المهمة وكأنك **فريق مكوّن من Senior UI/UX Designer + Senior Full-Stack Developer** مسؤول عن تسليم منصة تقنية احترافية جاهزة للإنتاج (Production-ready)، وليس مجرد تنفيذ متطلبات وظيفية بشكل حرفي. معايير القبول للعمل:
- أي واجهة تسلمها يجب أن تكون بمستوى يليق بعرضها كـ Case Study باستوديو تصميم عالمي — لا نتائج "تعمل بس" (Functional only) بل نتائج **جميلة، متسقة، ومدروسة بصرياً**.
- افترض دائماً أن هناك "مراجع جودة (QA/Design Reviewer)" سيفحص كل صفحة بعد تسليمك — راجع عملك بنفس هذه العين الناقدة قبل التسليم (تناسق الألوان، المسافات، الحركات، سلوك Hover/Active، اكتمال الترجمة) بدل انتظار ملاحظات لاحقة.
- عند التعارض بين "الأسرع بالتنفيذ" و"الأفضل بصرياً/تجربة المستخدم"، اختر الخيار الأفضل احترافياً ما لم يُذكر خلاف ذلك صراحة.

---

## 0) السياق العام للمشروع (يجب فهمه قبل أي تنفيذ)
هذا الجزء (الواجهات) هو **جزء واحد فقط** من نظام متكامل، والعمل الحالي على كود **Odoo تحديداً** الذي يمثل نصف النظام. يجب فهم الصورة الكاملة قبل التنفيذ:

- **الفكرة العامة**: نظام لأتمتة عملية جمع المواد القابلة لإعادة التدوير، يربط بين 4 أطراف: **المستخدم (البائع)** → **السائق** → **المستودع (Odoo)** → **المعمل (Factory)**.
- **تدفق العملية (Flow)**:
  1. المستخدم يعرض مواده القابلة لإعادة التدوير للبيع عبر التطبيق (Flutter).
  2. يُرسل سائق لاستلام هذه المواد من المستخدم.
  3. السائق يُسلّم المواد إلى **مستودع** — والمستودعات هذه مُدارة بالكامل داخل **Odoo** (تسجيل الكميات، الأصناف، المخزون).
  4. المعامل (Factories) تطلب مواد من المستودعات، ويتم شحن المواد من المستودع إلى المعمل حسب الطلب.
- **البنية التقنية (Architecture)**:
  - **Odoo**: يمثل نظام إدارة المستودعات والمخزون والمواد ومزامنة الأصناف (Plastic, Paper, Metal, Glass...) — هذا هو الجزء الذي يجري العمل عليه حالياً.
  - **NestJS Backend**: هو الطبقة الوسيطة (Backend الرئيسي) التي تدير: المستخدمين، السائقين، التطبيق (Auth, Orders, Tracking...)، وتتواصل مع Odoo عبر API/Webhooks لمزامنة البيانات (حالة الطلبات، المخزون، حركة الشحن بين المستودع والمعمل).
  - **الربط بينهما**: أي عملية تحدث بـ Odoo (استلام مواد، شحن لمعمل، تحديث مخزون) يجب أن تنعكس/تتزامن مع NestJS backend والعكس، بحيث تكون البيانات موحّدة بين النظامين وليست معزولة.

**مهم عند التنفيذ الحالي**: العمل الآن يخص كود **Odoo نفسه فقط** (وليس واجهات الويب المذكورة بالأقسام السابقة، ولا NestJS) — لكن أي تصميم/تعديل بـ Odoo يجب أن يُبنى بشكل يسهّل التكامل لاحقاً مع NestJS (مثال: حقول أو Endpoints قابلة للاستدعاء الخارجي، عدم كسر أي علاقة قد تحتاجها المزامنة).

---



- **Admin**: أي حساب برول Admin → يظهر له خيار "Dashboard" في القائمة/الحساب، ويفتح على لوحة تحكمه.
- **مستخدم عادي (بدون رول)**: يظهر له في الـ Navbar: الرئيسية، حول، اتصل بنا، **الوظائف (Jobs)**، Applications الخاصة به (My Applications)، الحساب، تسجيل الخروج.
  - لا يظهر له خيار "Dashboard" داخل صفحة الحساب إطلاقاً حتى يتم إسناد Role + Warehouse له.
- **موظف (بعد إسناد Role + Warehouse)**:
  - يُخفى خيار "الوظائف" نهائياً من الـ Navbar.
  - يبقى ظاهر له فقط "My Applications".
  - **مهم**: تأكد أن لا يوجد أي زر/رابط في أي صفحة بالموقع (كارد، CTA، Footer...) يوجّه الموظف لصفحة الوظائف. يجب فحص كل الصفحات.

## 2) صفحة الوظائف (Jobs)
- عرض الوظائف على شكل **Cards**.
- تفريق واضح بين الوظائف المنشورة (Published) وغير المنشورة (Unpublished) — تظهر للـ Admin فقط حالة "غير منشور"، والمستخدم العادي يرى المنشور فقط.
- تصميم الكارد يتبع نفس الهوية البصرية (انظر قسم 5).

## 3) قواعد العمل الثابتة (Business Rules — Enforce على مستوى Backend + Frontend)
- لا يمكن للأدمن إنشاء مستودع بنفس (كود المستودع) أو (اسم المستودع) لمستودع موجود مسبقاً.
- كل موظف له **رول واحد فقط** — يمنع إسناد أكثر من رول لنفس الموظف.
- كل مستودع له **مدير واحد فقط** (Manager unique per warehouse).
- كل مستخدم/موظف له **رقم وطني (National ID) فريد** و**بريد إلكتروني فريد** — لا تكرار على مستوى النظام كامل (Users + Employees).

## 4) إصلاحات تقنية إلزامية
- **مشكلة CSRF عند تسجيل الخروج**:
  - الخطأ الحالي: `400 Bad Request - Session expired (invalid CSRF token)` يظهر أحياناً عند Logout.
  - المطلوب: ضمان توليد/تجديد CSRF token بشكل صحيح مع كل session، ومعالجة انتهاء الجلسة بشكل نظيف بدون رمي هذا الخطأ لأي مستخدم أو موظف عند تسجيل الخروج (بأي سيناريو).
- **التوجيه بعد العمليات (Redirects)**:
  - بعد **Logout** (لأي دور: مستخدم/موظف/أدمن) → التوجيه حصراً لصفحة **Login**.
  - بعد **Login** أو **إنشاء حساب (Register)** ناجح → التوجيه حصراً لصفحة **Home**.

## 5) التصميم البصري (UI Design)
**المرجع**: الصورة المرفقة (Hero section لمنصة "دورها") — نفس الهوية تُطبّق على: الرئيسية، حول، الحساب، اتصل بنا، وصفحات Login/Register (مع صورة خلفية واحدة فقط وهي `hero4` من مجلد الصور).

- **الألوان**: خلفية داكنة (Navy/Almost-black: `#0B1220` تقريباً)، أخضر Primary زاهي (`#1FB876` تقريباً) للأزرار والعناصر البارزة، بطاقات بخلفية أفتح قليلاً من الخلفية الأساسية مع حدود ناعمة شفافة.
- **الخط والاتجاه**: RTL كامل بالعربي، خط عربي واضح (Cairo/Tajawal)، دعم تبديل EN بخط لاتيني مناسب.
- **عناصر متكررة**: بادج دائري بخلفية شفافة خضراء للأيقونات، بطاقات إحصائيات (Stats cards) بأرقام كبيرة بارزة، Divider أخضر قصير تحت العناوين.
- **حركات (Animations)**: انتقالات ناعمة (fade/slide) عند تحميل الأقسام، hover effect خفيف على الأزرار والبطاقات (scale/glow بسيط)، سلايدر صور بحركة سلسة.
- **الإحساس بالواقعية**: خلفيات بها تدرجات + نقوش شبكية خفيفة (كما بالصورة)، ظلال ناعمة على البطاقات لإعطاء عمق (لا تصميم مسطح بالكامل).
- **البيانات**: كل الأرقام والرسم البياني بالرئيسية (عدد الموظفين، المستودعات، الطلبات، الأطنان المُعاد تدويرها، نسبة النمو) يجب أن تُجلب من قاعدة البيانات الفعلية عبر API — **ممنوع أي رقم Static/Mock**.

## 6) الترجمة (i18n)
- ترجمة كاملة 100% لكل نص بالموقع (AR/EN) بدون استثناء — بما فيها:
  - كل الـ Placeholders في الحقول.
  - صفحات: تسجيل الدخول، تعيين/استعادة كلمة السر، إنشاء الحساب.
- عند التبديل EN ↔ AR يجب ألا يتبقى أي نص Hardcoded بلغة واحدة.

## 7) قالب إشعار البريد الإلكتروني
- تصميم إشعار البريد لأي إجراء (تفعيل حساب، إشعار عام...) يجب أن يتبع **نفس تصميم** إيميل "رمز التحقق (OTP)" الحالي (نفس الهيدر، الألوان، الفوتر، الشعار).

## 8) الاستجابة (Responsiveness)
- تصميم Responsive حقيقي وليس شكلي، لكل الشاشات (موبايل صغير/متوسط/تابلت/ديسكتوب).
- **لا يُسمح بإخفاء أو قصّ أي زر** بالموبايل — كل الأزرار يجب أن تظهر بترتيب مناسب (Stack عمودي، Bottom sheet، أو Menu حسب السياق) دون فقدان أي وظيفة موجودة بالديسكتوب.

## 9) الوضع الليلي/النهاري (Dark / Light Mode)
- إضافة زر تبديل (Toggle) بين **Dark Mode** و **Light Mode** على كل صفحات الموقع (وليس فقط الرئيسية).
- **Dark Mode**: نفس الهوية الحالية (خلفية داكنة Navy + أخضر Primary، كما بالصورة المرفقة).
- **Light Mode**: تصميم احترافي مطابق بنفس هيكلة العناصر (Cards, Navbar, Buttons, Hero) لكن بخلفية فاتحة نظيفة (أبيض/رمادي فاتح جداً) مع الحفاظ على نفس الأخضر Primary كلون مميز، وتباين ألوان واضح (Contrast) لسهولة القراءة، وظلال ناعمة بدل التوهج (Glow) المستخدم بالـ Dark.
- التبديل بين الوضعين يجب أن يكون **فوري (بدون Reload)** ويُحفظ اختيار المستخدم (Persist) بحيث يبقى نفس الوضع عند إعادة الدخول.
- تطبيق الترجمة (AR/EN) يعمل بشكل مستقل تماماً عن Dark/Light — أي دمج بين (اللغة المختارة × الوضع المختار) يجب أن يعمل بدون تعارض في أي من الحالات الأربع (AR-Dark, AR-Light, EN-Dark, EN-Light).

## 10) الجانب التقني (Code Quality & Architecture)
- **Tailwind CSS**: استخدام Tailwind كأساس للتنسيق بالكامل، مع تعريف الألوان (Primary/Background/Text لكل من Dark/Light) داخل `tailwind.config` كـ CSS Variables/Theme tokens بدل تكرار الألوان يدوياً بكل Component.
- **الديناميكية**: 
  - الألوان، الخطوط، والـ Spacing تُدار عبر متغيرات مركزية (Design Tokens) وليس Hardcoded داخل الكومبوننتات.
  - أي نص، رقم، أو حالة (Published/Unpublished, Role, Warehouse...) تُجلب Dynamic من الـ API — بدون أي Static/Mock data متبقي بأي واجهة.
- **تنظيم الكود**:
  - فصل واضح بين: Components (UI بحتة)، Hooks/Logic، Services (API calls)، Types/Interfaces، Translations (i18n files منفصلة لكل لغة).
  - أسماء ملفات ومتغيرات واضحة ومتسقة (Consistent naming convention).
  - إعادة استخدام Component واحد للأنماط المتكررة (مثال: Card واحد قابل لإعادة الاستخدام للوظائف/الإحصائيات بدل تكرار الكود).
- **معالجة الأخطاء**: قبل التنفيذ، افحص الكود الحالي وحدد المشاكل الموجودة (تكرار كود، حالات غير معالجة، تعارضات CSS بين الوضعين، تسريبات في الـ State) واذكرها بإيجاز ثم عالجها ضمن نفس التنفيذ.
- **Responsive**: كل واجهة (بما فيها الجديدة الخاصة بـ Dark/Light) تُختبر منطقياً على: موبايل صغير، موبايل كبير، تابلت، ديسكتوب — بدون فقدان أي زر أو عنصر تفاعلي.

## 11) تنبيه هام: مشاكل ظهرت فعلياً بعد التنفيذ السابق (يجب معالجتها تحديداً)
عند تنفيذ سابق لهذه المتطلبات، ظهرت المشاكل التالية بالنتيجة الفعلية على الموقع، ويجب الانتباه لها وعدم تكرارها:

- **صفحة "الحساب" (My Account) غير احترافية بصرياً**: التصميم الحالي بسيط جداً ولا يعكس نفس مستوى الهوية البصرية المطلوبة (قسم 5). المطلوب تصميم عصري وملفت فعلياً لهذه الصفحة تحديداً، وليس مجرد Card بسيط بمعلومات نصية:
  - استخدام نفس نظام الألوان والبطاقات والظلال والحركات المستخدمة بالرئيسية.
  - ترتيب بصري واضح للمعلومات (صورة/Avatar، الاسم، الدور/Badge، الإحصائيات كبطاقات منفصلة بتصميم متسق، وليس أرقام عارية بلا تنسيق).
  - تجنب أي عنصر يبدو "غير مكتمل" أو Default styling من Odoo بدون تخصيص.
- **الترجمة لم تُطبّق فعلياً على صفحات تسجيل الدخول وإنشاء الحساب**: رغم ذكرها بالمتطلبات (قسم 6)، هذه الصفحات ظهرت بدون ترجمة كاملة. المطلوب:
  - التأكد الفعلي (وليس الافتراض) أن كل نص/Label/Placeholder/رسالة خطأ في صفحات: تسجيل الدخول، إنشاء الحساب، استعادة كلمة السر — مترجمة بالكامل AR/EN.
  - قبل اعتبار المهمة منتهية، افحص هذه الصفحات تحديداً (View source / Inspect) وتأكد عدم وجود أي نص Hardcoded بلغة واحدة نسيت ترجمته.
  - نفس الفحص يجب أن يُطبّق على صفحة "الحساب" (My Account) لضمان أنها مترجمة بالكامل هي الأخرى.

**ملخص المطلوب من هذا القسم**: لا تكتفِ بتطبيق التصميم/الترجمة على الصفحة الرئيسية فقط ثم افتراض أن باقي الصفحات (حساب، تسجيل دخول، إنشاء حساب) ستُطبّق تلقائياً — كل صفحة يجب تدقيقها بشكل منفصل والتأكد من مطابقتها الفعلية.

## 12) صفحة تسجيل الدخول: إزالة قائمة "الحسابات المحفوظة" وتثبيت الخلفية
- حالياً عند فتح صفحة Login تظهر قائمة منبثقة بأسماء المستخدمين الذين سبق تسجيل الدخول بهم (Administrator, ahmad, mohamad...) مع خيار "Use another user" — هذه ميزة افتراضية من Odoo (Saved Sessions/Database Selector) وتُعطي شكل غير احترافي وغير متسق بحسب من فتح المتصفح.
  - **المطلوب**: تعطيل/إخفاء هذه القائمة نهائياً، وعرض **نموذج تسجيل الدخول مباشرة دائماً** (حقل Email/رقم وطني + كلمة السر) بغض النظر عن الجلسات السابقة على نفس الجهاز.
- **الخلفية**: لاحظت أن الصورة الخلفية بصفحة Login متغيّرة (Slideshow/Carousel) — هذا **خطأ**، فكما تم التوضيح سابقاً (قسم 5) يجب أن تكون **صورة خلفية ثابتة واحدة فقط** (`hero4`) بدون أي تبديل أو حركة سلايدر في صفحتي Login وRegister تحديداً (السلايدر المتحرك مسموح فقط بالرئيسية).

## 13) الاستقلالية بالحكم التصميمي (Design Autonomy)
- لا تنتظر توجيهاً تفصيلياً مني لكل عنصر بصري صغير. أي صفحة أو مكوّن **لم يُذكر تصميمه بالتفصيل صراحة** بهذا البرومبت، يجب أن يُصمَّم **تلقائياً باحترافية** بالاعتماد على نفس هوية الموقع ومبادئ الـ UX/UI السليمة (تباين ألوان مناسب، تباعد متسق Spacing، محاذاة صحيحة، تجربة استخدام سلسة)، دون أن يبدو Default/غير مكتمل أو Placeholder.
- عند إضافة أي صفحة/مكوّن جديد لاحقاً (حتى لو لم يُطلب تصميمه تحديداً)، طبّق عليه معايير قسم 5 (الألوان، البطاقات، الحركات) وقسم 9 (Dark/Light) تلقائياً كقاعدة عامة للمشروع، لا كاستثناء يحتاج طلب منفصل.
- قبل تسليم أي واجهة، افترض دور "مراجع تصميم UX/UI" وافحص: هل هذا العنصر يبدو كأنه جزء طبيعي من نفس الموقع؟ هل يوجد تفاوت غير مبرر بالتنسيق مقارنة ببقية الصفحات؟ عالج أي تفاوت تلقائياً دون انتظار ملاحظة مني.

## 14) تفاصيل بصرية دقيقة + حرية إبداعية كاملة بالتنفيذ
بالرجوع للصورة المرجعية المرفقة، انتبه لهذه التفاصيل الدقيقة وطبّقها كمعيار عام بكل الموقع، دون أن أحتاج لذكر كل عنصر بشكل منفصل مستقبلاً:

- **الخط الأخضر الفاصل (Accent Underline)**: تحت العناوين الرئيسية للأقسام (مثل "كيف تعمل") يوجد خط قصير سميك بلون أخضر Primary يعمل كفاصل بصري مميز. طبّق هذا العنصر تلقائياً تحت **كل عنوان رئيسي لأي قسم بأي صفحة** بالموقع (وليس الرئيسية فقط) كتوقيع بصري ثابت للهوية.
- **اختيار الخطوط (Typography)**: اختر بنفسك زوج خطوط احترافي يليق بهوية "منصة تقنية/بيئية حديثة":
  - العربي: خط Geometric واضح وعصري (مثل Cairo أو Tajawal) للعناوين، مع نفس العائلة أو IBM Plex Arabic للنصوص الفرعية لضمان القراءة المريحة.
  - الإنجليزي: خط Sans-serif حديث متوافق بصرياً مع الخط العربي بنفس الوزن البصري (مثال: Inter أو Poppins للعناوين).
  - اختر أوزان مختلفة (Bold للعناوين، Regular/Medium للنصوص) لخلق تدرج بصري واضح بالأهمية.
- **مواقع الحقول والعناصر (Layout)**: التزم بنفس منطق التوزيع بالصورة المرجعية — عنوان + وصف بجهة، وسائط بصرية (صورة/كارد) بالجهة المقابلة، مع محاذاة واتساق بالـ Padding/Margin بين كل الأقسام (لا تفاوت بالمسافات بين قسم وآخر).
- **الأيقونات**: أيقونات Outline بسيطة ونظيفة (بنفس ستايل الأيقونات بالصورة: شخص، منزل/مستودع، حقيبة، إعادة تدوير)، محاطة ببادج دائري شفاف أخضر، بنفس المقاس بكل الصفحات.

**التوجيه العام (مهم)**: لا تنتظر مني وصف كل عنصر جمالي بالتفصيل. أعطِ الموقع طابعاً **مميزاً وجذاباً بصرياً (Wow factor)** يليق بمنصة تقنية احترافية حقيقية — توازن بين الفخامة والبساطة، تناسق كامل بالألوان والمسافات والحركات، وكأن التصميم أُنجز من قِبل استوديو تصميم UI/UX متخصص. الإبداع بالتفاصيل (التدرجات، الظلال، الحركات الدقيقة) متروك لك طالما التزمت بالهوية الأساسية (الألوان، الخط الأخضر الفاصل، نمط البطاقات) المذكورة بهذا البرومبت.

## 15) حالة التفاعل بعناصر الـ Navbar (Hover / Active State)
- عند مرور الماوس (Hover) على أي عنصر بالـ Navbar (الرئيسية، حول، اتصل بنا، وظائف، الحساب، الطلبات...): يتلوّن العنصر تلقائياً (تغيّر لون النص/الخلفية للأخضر Primary أو درجة منه) بحركة انتقالية ناعمة (Smooth transition، ليس تغيير مفاجئ).
- عند الدخول لصفحة معينة (مثال: دخلت لصفحة "وظائف"): يبقى عنصر "وظائف" بالـ Navbar **ملوّناً/مميزاً (Active State)** طوال بقاء المستخدم بهذه الصفحة، ويعود للون العادي فقط عند الانتقال لصفحة أخرى (حيث يصبح العنصر الجديد هو الـ Active).
- هذا السلوك (Hover + Active) يُطبّق بشكل موحّد على **كل عناصر الـ Navbar بكل الصفحات** دون استثناء.

## 16) تذكير بطبيعة المشروع لتوجيه الطابع الجمالي العام
هذا الموقع هو **منصة لإدارة مستودعات المواد القابلة لإعادة التدوير** (وليس موقعاً عاماً) — يجب أن ينعكس هذا بالطابع البصري لكل صفحة (الرئيسية، حول، الحساب، وظائف، الطلبات، وأي صفحة أخرى):
- إحساس بصري "تقني-بيئي" (Tech + Eco) يوحي بالتنظيم، الاستدامة، والذكاء بإدارة العمليات — وليس مجرد موقع تعريفي عادي.
- كل صفحة (حتى الداخلية كصفحة الطلبات) يجب أن تحمل نفس مستوى الاحترافية والجاذبية البصرية المطبّقة بالرئيسية، بما يعكس أن هذا نظام تشغيلي حقيقي لإدارة عمليات إعادة تدوير على نطاق واسع، لا مجرد Landing Page.
- الهدف النهائي: أي زائر أو مستخدم يفتح أي صفحة بالموقع يشعر فوراً أنه أمام **منصة احترافية متخصصة بإدارة المستودعات وإعادة التدوير**، بمستوى تصميم يليق بهذا التخصص.

## 17) مرجع تصميم إضافي (Layout Reference) — يُعتمد كمعيار هيكلي للصفحات
بالإضافة للهوية اللونية بقسم 5، هذه الصور المرفقة تمثل **مرجع هيكلي (Structure/Layout)** لجودة وترتيب العناصر المطلوبة بكل صفحة — طبّق نفس منطق التنظيم هذا، لكن بألوان هوية "دورها" (الأخضر/الداكن وليس الألوان بالصور):

- **صفحة تسجيل الدخول / إنشاء حساب**: تقسيم الشاشة نصفين — نصف يحتوي النموذج (Form) بخلفية بيضاء/داكنة نظيفة حسب الوضع (Light/Dark)، والنصف الآخر أو الخلفية العامة تحتوي عنصر بصري طبيعي (نباتات/تدرج) بأسفل الشاشة كتفاصيل زخرفية خفيفة. الحقول بحواف دائرية ناعمة (Rounded) مع Label واضح فوق كل حقل، وزر رئيسي بعرض كامل بلون Primary.
- **صفحة "الحساب" (My Account / Dashboard)**: تخطيط Sidebar ثابت بجهة (لوحة التحكم، معلوماتي، طلباتي، العناوين، طرق الدفع، الإعدادات، تسجيل الخروج) + منطقة رئيسية تحتوي: بطاقة ترحيب شخصية (صورة/اسم + رسالة ترحيب)، بطاقات إحصائيات مصغّرة (عدد الطلبات، الحالة...)، وجدول/قائمة "آخر النشاطات" أسفلها.
- **صفحة "الوظائف" (Jobs)**: Header علوي فيه بحث + فلاتر (الموقع، نوع الوظيفة) بشكل Dropdown أنيق، تبويبات فرعية (الكل، دوام كامل، دوام جزئي، مؤقت)، وكل وظيفة تُعرض كصف/كارد أفقي يحتوي: أيقونة الشركة/القسم، المسمى الوظيفي، اسم الجهة، الموقع، Tags (نوع الدوام، الخبرة المطلوبة)، وزر "التقديم الآن" بجهة اليمين.
- **صفحة "طلباتي" (My Applications/Orders)**: عرض على شكل بطاقات أو صفوف، مع **Status Badge ملوّن** واضح لكل حالة (مكتمل=أخضر، جارية=أصفر/برتقالي، ملغي/مرفوض=أحمر، تحت المراجعة=أزرق) — نفس منطق الألوان الدلالية (Semantic Colors) هذا يُطبّق بكل مكان بالموقع فيه حالات/Status (طلبات المستخدم، حالة الوظيفة، حالة الموظف...).
- **الموبايل (كما بالصور اليمنى بالمرجع الثاني)**: بدل الـ Sidebar الثابت، تتحول القائمة الجانبية إلى Menu علوي (Hamburger/Header) مع نفس ترتيب الأقسام، وتبقى البطاقات والجداول بنفس المحتوى لكن بتخطيط عمودي (Stacked) متكيّف بالكامل.

**الخلاصة**: اعتمد هذا التنظيم الهيكلي (توزيع Sidebar، شكل الفلاتر، تصميم الـ Status Badges، تنسيق الكروت بالوظائف) كمعيار جودة، مدموجاً بالكامل مع هوية الألوان والخط الأخضر الفاصل المذكورة سابقاً بالبرومبت (وليس نسخ ألوان الصور المرجعية حرفياً).

## 18) تعزيز إضافي: التجاوب (Responsive) بشكل صارم
- كل صفحة تُختبر فعلياً (وليس افتراضياً) على 4 نقاط توقف (Breakpoints) على الأقل: موبايل صغير (~360px)، موبايل كبير (~430px)، تابلت (~768px)، ديسكتوب (1280px+).
- **لا يُقبل** أي عنصر يتم إخفاؤه بالموبايل فقط "لأنه ما يسع" — كل عنصر وظيفي (زر، فلتر، رابط) لازم يبقى متاحاً، وإذا لزم الأمر يُعاد ترتيبه (Stack عمودي، تحويل الفلاتر لـ Bottom Sheet/Modal، تصغير الأيقونات) بدل حذفه أو إخفائه.
- الجداول (مثل صفحة الطلبات) تتحول تلقائياً لبطاقات (Card view) بالموبايل بدل Scroll أفقي مزعج للجدول الكامل.
- النصوص العربية/الإنجليزية الطويلة لا تُقصّ أو تخرج عن حدود العنصر (overflow) بأي حجم شاشة — استخدام Truncation مع Tooltip عند الحاجة فقط للنصوص الطويلة جداً.

## 19) تقليل إمكانية فحص تصميم الموقع عبر Inspector (بحدود الممكن تقنياً)
**ملاحظة مهمة أولاً**: أي موقع ويب، مهما طُبّقت عليه إجراءات حماية، يبقى كوده (HTML/CSS/JS) قابلاً للوصول من طرف المتصفح بشكل أساسي لأن المتصفح يحتاج تحميله وتنفيذه ليعرض الصفحة — لا توجد طريقة تمنع الوصول الكامل 100% لمستخدم متمرس تقنياً. لكن يمكن تطبيق إجراءات **تُصعّب** الاستخدام العرضي/السريع لأدوات Inspector:
- **تعطيل الزر اليمين (Right-click)** على عناصر الصفحة لمنع "Inspect" السريع من القائمة المنسدلة.
- **تعطيل اختصارات لوحة المفاتيح** الشائعة لفتح أدوات المطوّر (F12, Ctrl+Shift+I, Ctrl+U لعرض المصدر) عبر JavaScript Event Listener.
- **تصغير وضغط الكود (Minification)** لملفات CSS/JS بالإنتاج (Production Build) بحيث يصعب قراءة/فهم البنية حتى لو تم فتحها.
- **تجنب أسماء Class/ID واضحة المعنى** بالإنتاج (استخدام Hashed class names كما يحدث تلقائياً مع Tailwind + build tools عند التفعيل الصحيح لـ purge/minify).
- **حماية الصور** (مثل hero4 وصور الوظائف) بمنع السحب المباشر (`draggable=false`) ووضع علامة مائية خفيفة إن كانت الصور أصلية للشركة.

اطبّق هذه الإجراءات كطبقة حماية إضافية (Deterrent) مع العلم أنها لا تُغني عن حماية الـ Backend/API نفسها (والتي تبقى الأهم أمنياً)، فهذه الإجراءات خاصة فقط بتصميم الواجهة (UI) وليست بديلاً عن أمان البيانات.

## 20) حساب Admin الافتراضي — دور مسند بتطبيق المستودعات
- حساب الأدمن الأساسي بالنظام (بيانات الدخول: `admin` / `admin`) يجب أن يكون له **دور مُسند فعلياً** داخل تطبيق "Recycling Warehouse" (وليس مجرد صلاحية Odoo Administration العامة بدون دور بالتطبيق نفسه).
- هذا الدور المُسند يجب أن يكون **"Administration"** كقيمة افتراضية (Default) لحساب الأدمن هذا تحديداً، بحيث عند فتح لوحة التحكم أو صفحة الحساب، يظهر دوره بوضوح كـ "Administration" بدل أن يبقى بدون دور أو Applicant كما ظهر سابقاً بالصورة (قسم 11).
- تأكد أن هذا الربط (User ↔ Role="Administration") يتم تلقائياً عبر بيانات التهيئة الأولية (Seed Data/Demo Data) عند تثبيت الموديول، وليس خطوة يدوية يجب تكرارها بكل بيئة جديدة.

**تنبيه (خارج نطاق التصميم لكن مهم للمتابعة)**: بيانات الدخول `admin`/`admin` مقبولة فقط لبيئة التطوير المحلية (Local/Dev)؛ عند الانتقال لبيئة الإنتاج (Production) يجب تغيير كلمة المرور لقيمة قوية قبل النشر.

---
**ملاحظة تنفيذية للـ AI**: نفّذ كل قسم كوحدة مستقلة، واعرض ملخص التغييرات فقط بدون شرح مطوّل لكل خطوة، للحفاظ على الوقت والتوكن.
